# 开发文档

## 项目状态

- 当前阶段：V1 本地可运行 Agent 原型；真实能力集中在工程经济学专项冲刺链路，正在准备向通用课程期末速成工作台迁移。
- 开发日期：2026-07-20。
- 产品方案来源：`期末粥加速器_V1方案.md`。
- 视觉参考：`浅色模式参考.png`、`深色模式参考.png`。
- 工作台背景：`浅色模式背景.png`、`深色模式背景.png`。

## 本次开发目标

实现可在浏览器中操作的课程复习闭环：

1. 进入课程工作台，查看课程状态、复习进度和 AI 建议。
2. 在复习主线中完成或开始学习任务。
3. 在刷题模块中提交答案，更新知识点掌握度并产生错题。
4. 在模拟卷模块中完成演练并查看评分结果。
5. 在笔记和错题本中维护学习信息。
6. 在 AI 伴学栏输入调整需求，查看并确认计划调整提案。
7. 使用本地 FastAPI 保存工程经济学学习状态，并接入可配置的模型服务。

## 产品方向校准

用户真正想要的是“期末速成”通用项目，而不是只服务 `工程经济学` 一门课的演示应用。后续开发应把当前已经跑通的工程经济学链路抽象成课程无关能力：

- 课程无关资料库：每门课程都有独立资料目录、解析缓存、资料状态和来源引用。
- 课程无关学习空间：每门课程都能生成 `assessmentProfile`、`knowledgePoints`、`tasks`、`practiceQuestions`、`mockQuestions`、`wrongAnswers`、`notes` 和 `messages`。
- 课程无关 Agent API：前端不再调用 `/api/engineering-economics/...`，而是统一调用 `/api/courses/{course_id}/...`。
- 课程无关速成策略：根据考试日期、目标分数、每日可用时间、已掌握程度和资料质量，自动生成 1-14 天冲刺计划。
- 课程无关闭环：资料解析 -> 考核画像 -> 摸底 -> 计划 -> 学习 -> 刷题 -> 模拟 -> 错题 -> 计划调整，都应落到同一套后端工作流和本地记忆。

## 实现记录

### 2026-07-20

- 已读取并确认产品方案、浅色模式参考图与深色模式参考图。
- 已建立项目级说明文档与本开发文档。
- 已初始化 `web`：React + TypeScript + Vite 前端工程，并引入 `lucide-react` 图标库。
- 已建立三栏课程工作台：左侧学习模块导航、课程列表、中间复习工作区、右侧常驻 AI 伴学栏。
- 已实现课程切换、多课程任务兜底和浏览器本地存储。不同课程的任务更新不会覆盖其他课程记录。
- 已实现复习主线任务状态更新、刷题作答反馈、模拟卷提交与结果讲评、笔记编辑、错题标记、资料导入展示和课程创建弹窗。
- 已实现 AI 伴学调整提案：识别“删除不考内容”“增加复习时间”等意图，用户确认后才应用计划调整。
- 已建立 `backend`：FastAPI、SQLite 初始化、CORS、本地健康检查、课程读取、计划读取、课程创建和调整提案接口。
- 已将页面语言设为中文，页面标题为“期末粥加速器 · 本地学习工作台”。

### 2026-07-20（背景与设置完善）

- 已将浅色、深色背景图分别部署到前端静态资源，并通过半透明面板、轻度模糊和主题化遮罩融入工作台。
- 已新增独立设置页，并将左下角“设置”导航与 AI 伴学栏的“打开 AI 设置”入口接通。
- 已支持 OpenAI、DeepSeek、GLM 和自定义 OpenAI 兼容服务的 Base URL、模型名、API Key、图片理解能力配置。
- 已支持配置本机保存、API Key 显隐、浅色/深色模式切换，以及本地后端的模型连通性测试与失败原因反馈。
- 后端新增 `POST /api/model-profiles/test`：以 `/models` 探测模型服务可达性，不保存用户提交的 API Key。

### 2026-07-20（工程经济学真实学习链路）

- 已扫描 `工程经济学期末复习` 下的 42 份资料，包括 PPT/PPTX、PDF、真题图片、课后练习图和 Excel 文件。
- 后端已解析 Markdown 总览和 PPTX 文本摘录，连同资料目录和真题题型观察作为模型生成的课程上下文。
- 本机模型通过 `backend/.env` 读取自定义 Base URL、API Key 和 `gpt-5.4-mini`；前端只读取模型名称和连接状态，不读取 API Key。
- 已新增工程经济学学习空间接口：资料索引、3 天计划生成、练习判题、模拟卷判题、错题写入、笔记/任务状态保存和 AI 伴学对话。
- 已生成并验证 3 天、每天 2 小时、目标 80+ 的学习空间：6 项任务、6 道定向练习题、3 道模拟题。
- 前端已切换到后端学习空间，不再展示数据结构演示课程；刷题提交后会更新掌握度和任务优先级，AI 伴学栏调用真实模型回复。

### 2026-07-22（通用化前代码盘点）

- 后端已新增归档能力：课程和错题删除后写入 `archived_items`，默认 7 天内可恢复，过期自动清理。
- 后端已新增资料解析缓存：`backend/data/material_cache` 保存解析 JSON 和 Office 转 PDF 预览缓存，并通过 `MATERIAL_ANALYSIS_VERSION` 控制刷新。
- 后端资料解析能力已从早期 PPTX/Markdown 扩展到 MarkItDown、内置 XLSX 预览、可选 Docling、可选 LibreOffice/soffice 转 PDF。
- 资料库已区分 AI 可读状态和浏览器预览状态，包括 `AI已解析`、`AI部分解析`、`AI未解析`、`无需解析`、`可预览`、`PDF预览`、`文本预览` 等。
- 前端已新增归档页、课程删除/恢复、错题删除/恢复、资料重新解析、资料预览弹窗、AI/预览状态标签、字体和字号设置。
- 前端仍以工程经济学学习空间为真实后端数据源；新建的其他课程只生成前端本地兜底工作区，没有资料导入、AI 画像、题库生成和 Agent 对话的完整后端链路。
- 真实 Agent 接口仍硬编码在 `/api/engineering-economics/...`，包括 workspace、materials、practice、mock、agent chat 和 rescan。
- 后端通用课程表目前只保存 `courses` 与简化版 `plan_tasks`，还没有通用资料、知识点、题目、错题、笔记、消息、提案等数据库结构。
- `POST /api/agent/adjustment-proposals` 是早期通用提案接口，但前端真实 AI 侧栏目前调用的是工程经济学专用 `/api/engineering-economics/agent/chat`。
- AI 伴学能基于当前工程经济学状态回答，但“自然语言提案 -> 用户确认 -> 后端改写计划 -> 保留调整记录”的动作闭环尚未真正完成。

## 当前实现边界

- 工程经济学页面依赖本地 FastAPI 服务；未启动后端时，前端会显示可理解的连接失败状态。
- 当前“可运行 Agent”主要是工程经济学专项 Agent，通用课程只具备课程卡片、基础任务和前端兜底展示。
- 资料解析已经能覆盖更多格式，但图片 OCR、扫描版 PDF OCR、逐页结构化引用、题目自动抽取和跨资料检索仍未形成稳定通用管线。
- 模型生成采用 JSON 模式；若模型偶发返回非结构化内容，后端会回退到同一份工程经济学资料驱动的确定性学习计划。
- AI 伴学可基于当前掌握度、任务和错题实时回答；自然语言自动“确认并改写计划”的动作编排仍可继续扩展。
- 前端仍有工程经济学文案和 API 命名耦合，例如搜索占位、资料接口、刷题接口、模拟卷接口和 Agent 对话接口。

## 通用期末速成化路线

### 第一阶段：后端课程空间通用化

目标：把工程经济学专项 workspace 变成任意课程都能拥有的本地学习空间。

- 新增或扩展通用数据模型：`courses`、`materials`、`material_chunks`、`knowledge_points`、`exam_blueprints`、`plan_tasks`、`questions`、`question_attempts`、`wrong_answers`、`notes`、`messages`、`adjustment_proposals`。
- 将 `COURSE_DIRECTORY`、`WORKSPACE_PATH`、`engineering_economics_workspace.json` 改为按 `course_id` 定位，例如 `backend/data/courses/{course_id}/workspace.json` 和 `backend/data/courses/{course_id}/materials/`。
- 把 `/api/engineering-economics/workspace` 迁移为 `/api/courses/{course_id}/workspace`。
- 把资料预览、资料重扫、练习作答、模拟卷提交、Agent 对话全部迁移为 `/api/courses/{course_id}/...`。
- 保留工程经济学作为第一门示例课程，但它应该走通用 API，而不是走特殊路径。

### 第二阶段：资料导入与课程画像通用化

目标：用户新建任意课程后，可以导入资料并得到课程画像。

- 前端新增真实资料导入入口，支持 PDF、PPT/PPTX、DOC/DOCX、XLS/XLSX、图片和 Markdown。
- 后端为每个资料生成解析记录：原文件路径、解析器、可读状态、预览状态、页码/幻灯片/工作表来源、文本片段和错误原因。
- 生成课程画像时不写死工程经济学，而是让模型从资料中抽取章节、知识点、题型、分值倾向、重点/不考范围和可追溯来源。
- 对资料质量做提示：哪些资料已进入 AI、哪些只可预览、哪些需要 OCR 或重新上传。

### 第三阶段：期末速成核心算法

目标：真正解决“考前几天如何速成”的问题。

- 新建课程时采集：考试日期、考试形式、目标分数、每日可用时间、剩余天数、基础水平、老师划重点、不考范围。
- 计划生成公式从工程经济学固定 `3 天 × 2 小时 × 80+` 改为通用参数：`考试权重 × 薄弱程度 × 目标分差 × 剩余时间压力 × 资料可信度`。
- 摸底题从固定题库改为动态生成：先 8-12 题快速定位水平，再根据错因更新掌握度。
- 刷题从固定 6 题改为按知识点、题型、难度和错因动态抽题/生题。
- 模拟卷从固定 3 题改为根据考试画像限时组卷，并输出得分、失分点、错因和下一轮计划。

### 第四阶段：Agent 动作闭环

目标：让 AI 伴学不只是回答，而能在用户确认后安全改计划。

- `agent/chat` 返回普通回答和可选结构化提案，不再只返回一段文本。
- 提案包含：调整类型、原因、受影响任务、调整前后时间、预计收益、风险、需要确认的动作。
- 新增 `POST /api/courses/{course_id}/adjustment-proposals/{proposal_id}/apply`，确认后由后端写回计划、错题或资料状态。
- 保存提案历史，支持撤销或恢复，避免 AI 静默修改。

### 第五阶段：产品体验完善

目标：让它像一个“期末周控制台”，而不是单课程 Demo。

- 首页支持多课程优先级排序：离考试近、目标差距大、薄弱点多的课程自动靠前。
- 增加今日总学习排程，把多门课的任务合成一个日程。
- 搜索框真正检索资料、知识点、笔记、错题和题目。
- 每门课显示“速成信心”：资料完整度、计划可行性、目标达成概率和主要风险。
- 保留本地优先：API Key、本地资料和学习记录默认不出本机；只有模型任务需要的片段发送给用户配置的模型服务。

## 验收记录

### 自动检查

- `web`：`npm run build` 通过。
- `web`：`npm run lint` 通过。
- `backend`：`python -m py_compile app/main.py app/study_service.py` 通过。
- 后端健康检查：`GET /api/health` 返回 `status: ok`。
- 后端冒烟接口：课程列表、数据结构复习计划、AI 调整提案接口均可响应。
- 工程经济学初始化：模型生成模式返回 42 份资料、6 项任务、6 道练习题、3 道模拟题；每天任务时长均为 120 分钟。

### 浏览器检查

- 桌面端：已验证页面非空、无框架错误覆盖层、控制台无警告和错误。
- 模拟卷闭环：开始模拟 -> 填写主观题 -> 提交并生成报告 -> AI 生成调整提案 -> 用户确认调整，状态均已更新。
- 多课程：切换到大学物理后，课程标题、课程信息和任务列表会同步切换。
- 移动端：在 `390 × 844` 视口下，页面宽度与文档宽度一致，无横向溢出；课程列表和 AI 伴学以侧栏方式收起。
- 设置功能：左侧导航和 AI 伴学栏均可打开设置页；自定义模型配置可保存，测试无效 Base URL 时会显示可理解的失败反馈并记录测试时间。
- 主题背景：在 `1600 × 1000` 视口中分别检查浅色和深色模式，`background-light.png`、`background-dark.png` 静态资源均返回 `200`。
- 工程经济学主线：桌面端验证资料库、3 天计划、模型连接状态和 AI 伴学首屏均正确渲染。
- 刷题闭环：选择正确答案后，后端返回基于资料的讲评，知识点掌握度由 78% 更新为 86%，页面无控制台错误。
- AI 对话：侧边栏实际请求本机模型服务并返回工程经济学复习建议。
- 移动端复查：在 `390 × 844` 视口下，总览无横向溢出，底部导航和 AI 入口可用。
